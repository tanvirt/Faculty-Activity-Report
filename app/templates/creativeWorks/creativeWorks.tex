\section{CREATIVE WORKS OR ACTIVITIES}

I developed/help develop {{ array.length }} major software systems:

\begin{itemize}

{% for work in array %}
\item {{ work.name | capitalize }}: {{ work.description }}
{ {{ work.website -}} } {{ work.date|date('M-d-Y') }}
(joint effort with {% for joint in work.jointEfforts %}{{ joint }}{% if !loop.last %}, {% endif %}{% endfor %})
{% endfor %}

\end{itemize}